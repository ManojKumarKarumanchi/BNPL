{
  "name": "grabcredit-checkout",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo 'Linting not configured. Install ESLint to enable.' && exit 0",
    "lint:fix": "echo 'Linting not configured. Install ESLint to enable.' && exit 0",
    "typecheck": "echo 'TypeScript not configured. This is a JavaScript project.' && exit 0",
    "test": "echo 'Tests not configured. Install Vitest or Jest to enable.' && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "framer-motion": "^11.0.8"
  },
  "devDependencies": {
    "@heroicons/react": "^2.1.1",
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "vite": "^5.1.4"
  }
}
